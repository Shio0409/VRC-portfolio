# 作業方針

- 作成・修正作業が完了したら、変更に応じた確認を行い、今回の変更をコミットしてGitHubの`origin`へ通常のpushを行う。ユーザーから継続的に承認されているため、作業ごとのpush確認は不要。
- ユーザーが調査のみ・変更禁止・push不要等を指定した場合は、その指示を優先する。
- Webコード、使用中の素材、仕様書、`consept`のコンセプト資料をGit管理する。Unityプロジェクト`siofel 2601`は含めない。
- 既存のユーザー変更を取り消したり、無関係な変更をコミットに混ぜたりしない。
- Webルートは`web`。実行方法は`web/README.md`、サイト仕様は`portfolio sitedesign.md`を参照する。
